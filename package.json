{
  "name": "ynab-csv",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "tsc",
    "copy:html": "cp src/static/* dist/",
    "build": "npm run clean && npm run compile && npm run copy:html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && http-server dist -o"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "typescript": "^5.8.2"
  }
}
