{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "test": "vitest",
    "dev": "vite",
    "build": "npm run clean && vitest run && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/papaparse": "^5.3.15",
    "typescript": "~5.7.2",
    "vite": "^6.2.0",
    "vitest": "^3.1.1"
  },
  "dependencies": {
    "papaparse": "^5.5.2"
  }
}
